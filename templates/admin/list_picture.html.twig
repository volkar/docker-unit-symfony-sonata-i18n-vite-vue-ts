{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}

    {% if object.picture %}
    {{ sonata_thumbnail(object.picture, 'admin', {'style': 'width: 80px; height: auto;'}) }}
    {% endif %}

{% endblock %}